{% extends '::base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}

    <link rel="stylesheet" href="{{ asset('bundles/user/css/login.css') }}" />
{% endblock %}

{% block body %}
    <section class="login">
        <article>
            <h1>Ah crap!</h1>
            <h2>The server returned a "{{ status_code }} {{ status_text }}".</h2>

        </article>
    </section>
    {{ render(controller('EventBundle:Event:_upcomingEvents'), 'max' : 1) }}
{% endblock %}